{
    "version": 2,
    "builds": [
      { "src": "api/**/*.js", "use": "@now/node" }
    ],
    "routes": [
      { "handle": "filesystem" },
      {
        "src": "/(.*)",
        "dest": "/api/$1",
        "methods": ["GET", "POST", "PUT", "DELETE"]
      }
    ]
  }
  